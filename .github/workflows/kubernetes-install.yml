name: Kubernetes and Infra Install

permissions:
  contents: read

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment Name"
        required: false
        default: "dev"
      tf-prepare:
        type: boolean
        default: false
      tf-apply:
        type: boolean
        default: false
      tf-destroy:
        type: boolean
        default: false
      pg-install:
        type: boolean
        default: false
      consul-install:
        type: boolean
        default: false
      zookeeper-install:
        type: boolean
        default: false
      kafka-install:
        type: boolean
        default: false
      rabbitmq-install:
        type: boolean
        default: false
      cassandra-install:
        type: boolean
        default: false


jobs:
  running-inputs:
    permissions:
      contents: read
    environment: "${{github.event.inputs.environment}}"
    runs-on: ubuntu-latest
    steps:
      - name: Show inputs
        env:
          ENV_VAR: ${{ vars.ENV_VAR }}
          DEPLOY_ENVIRONMENT: ${{ github.event.inputs.environment }}
          TF_PREPARE_INPUT: ${{ github.event.inputs.tf-prepare }}
          INPUTS_JSON: ${{ toJSON(github.event.inputs) }}
          EKS_CLUSTER_NAME: ${{ vars.TF_VAR_EKS_NEW_CLUSTERNAME }}
        run: |
          echo "Deploying to: $DEPLOY_ENVIRONMENT"
          echo "tf-prepare: $TF_PREPARE_INPUT"
          echo "JSON: $INPUTS_JSON"
          echo "EKS cluster name: $EKS_CLUSTER_NAME"
  tf-prepare:
    permissions:
      contents: read
    needs: running-inputs
    environment: "${{github.event.inputs.environment}}"
    if: github.event.inputs.tf-prepare == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: 1.12.2

      - name: Verify tools
        run: |
          terraform version
          kubectl version --client
          helm version
          aws --version

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@e1253824e5c10ff9df46874f81ed3ec929e19cfd # v6.3.0
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION || 'us-east-1' }}

      - name: Terraform Init and Plan
        env:
          EKS_CLUSTER_NAME: ${{ vars.TF_VAR_EKS_NEW_CLUSTERNAME }}
        run: |
          cd ./kubernetes-terraform
          terraform init
          if ! terraform workspace list | grep -q "$EKS_CLUSTER_NAME"; then
          terraform workspace new "$EKS_CLUSTER_NAME"
          echo "Create new env for TF state"
          fi
          terraform workspace select "$EKS_CLUSTER_NAME"
          terraform plan -var "EKS_NEW_CLUSTERNAME=$EKS_CLUSTER_NAME" -out=tfplan

      - name: Upload Terraform plan
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: tfplan-${{ vars.TF_VAR_EKS_NEW_CLUSTERNAME }}
          path: kubernetes-terraform/tfplan
          retention-days: 1

  tf-apply:
    permissions:
      contents: read
    needs: [running-inputs, tf-prepare]
    environment: "${{github.event.inputs.environment}}"
    if: github.event.inputs.tf-apply == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: 1.12.2

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@e1253824e5c10ff9df46874f81ed3ec929e19cfd # v6.3.0
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION || 'us-east-1' }}

      - name: Download Terraform plan
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: tfplan-${{ vars.TF_VAR_EKS_NEW_CLUSTERNAME }}
          path: kubernetes-terraform

      - name: Terraform Apply
        env:
          EKS_CLUSTER_NAME: ${{ vars.TF_VAR_EKS_NEW_CLUSTERNAME }}
        run: |
          cd ./kubernetes-terraform
          terraform init
          if ! terraform workspace list | grep -q "$EKS_CLUSTER_NAME"; then
          terraform workspace new "$EKS_CLUSTER_NAME"
          echo "Create new env for TF state"
          fi
          terraform workspace select "$EKS_CLUSTER_NAME"
          terraform apply -auto-approve tfplan

  tf-post-install:
    permissions:
      contents: read
    needs: tf-apply
    environment: "${{github.event.inputs.environment}}"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: 1.12.2

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@e1253824e5c10ff9df46874f81ed3ec929e19cfd # v6.3.0
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION || 'us-east-1' }}

      - name: Configure kubeconfig for EKS
        env:
          AWS_REGION: ${{ vars.AWS_REGION || 'us-east-1' }}
          EKS_CLUSTER_NAME: ${{ vars.TF_VAR_EKS_NEW_CLUSTERNAME }}
        run: |
          aws eks update-kubeconfig --region "$AWS_REGION" --name "$EKS_CLUSTER_NAME"

      - name: Terraform Post Install
        env:
          EKS_CLUSTER_NAME: ${{ vars.TF_VAR_EKS_NEW_CLUSTERNAME }}
        run: |
          cd ./kubernetes-terraform/post-configuration/
          terraform init
          if ! terraform workspace list | grep -q "$EKS_CLUSTER_NAME"; then
          terraform workspace new "$EKS_CLUSTER_NAME"
          fi
          terraform workspace select "$EKS_CLUSTER_NAME"
          terraform apply -var "EKS_NEW_CLUSTERNAME=$EKS_CLUSTER_NAME" -auto-approve

  tf-destroy:
    permissions:
      contents: read
    if: github.event.inputs.tf-destroy == 'true'
    environment: "${{github.event.inputs.environment}}"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: 1.12.2

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@e1253824e5c10ff9df46874f81ed3ec929e19cfd # v6.3.0
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION || 'us-east-1' }}

      - name: Terraform Destroy
        env:
          EKS_CLUSTER_NAME: ${{ vars.TF_VAR_EKS_NEW_CLUSTERNAME }}
        run: |
          cd ./kubernetes-terraform
          terraform init
          if ! terraform workspace list | grep -q "$EKS_CLUSTER_NAME"; then
          terraform workspace new "$EKS_CLUSTER_NAME"
          echo "Create new env for TF state"
          fi
          terraform workspace select "$EKS_CLUSTER_NAME"
          terraform destroy -var "EKS_NEW_CLUSTERNAME=$EKS_CLUSTER_NAME" -auto-approve

  pg-install:
    permissions:
      actions: write
    needs: tf-post-install
    environment: "${{github.event.inputs.environment}}"
    if: github.event.inputs.pg-install == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Show inputs
        env:
          PG_INSTALL_INPUT: ${{ github.event.inputs.pg-install }}
          INPUTS_JSON: ${{ toJSON(github.event.inputs) }}
        run: |
          echo "tf-prepare: $PG_INSTALL_INPUT"
          echo "JSON: $INPUTS_JSON"

      - name: Trigger PG install workflow
        uses: actions/github-script@d746ffe35508b1917358783b479e04febd2b8f71 # v9.0.0
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'postgres-new-cluster-install.yml',
              ref: 'main'
            })

  consul-install:
    permissions:
      actions: write
    needs: tf-post-install
    environment: "${{github.event.inputs.environment}}"
    if: github.event.inputs.consul-install == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Show inputs
        env:
          CONSUL_INSTALL_INPUT: ${{ github.event.inputs.consul-install }}
          INPUTS_JSON: ${{ toJSON(github.event.inputs) }}
        run: |
          echo "tf-prepare: $CONSUL_INSTALL_INPUT"
          echo "JSON: $INPUTS_JSON"

      - name: Trigger Consul install workflow
        uses: actions/github-script@d746ffe35508b1917358783b479e04febd2b8f71 # v9.0.0
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'consul-new-cluster-install.yml',
              ref: 'main'
            })

  zookeeper-install:
    permissions:
      actions: write
    needs: tf-post-install
    environment: "${{github.event.inputs.environment}}"
    if: github.event.inputs.zookeeper-install == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Show inputs
        env:
          ZOOKEEPER_INSTALL_INPUT: ${{ github.event.inputs.zookeeper-install }}
          INPUTS_JSON: ${{ toJSON(github.event.inputs) }}
        run: |
          echo "tf-prepare: $ZOOKEEPER_INSTALL_INPUT"
          echo "JSON: $INPUTS_JSON"

      - name: Trigger Zookeeper install workflow
        uses: actions/github-script@d746ffe35508b1917358783b479e04febd2b8f71 # v9.0.0
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'zookeeper-new-cluster-install.yml',
              ref: 'main'
            })

  kafka-install:
    permissions:
      actions: write
    needs: [tf-post-install, zookeeper-install]
    environment: "${{github.event.inputs.environment}}"
    if: github.event.inputs.kafka-install == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Show inputs
        env:
          KAFKA_INSTALL_INPUT: ${{ github.event.inputs.kafka-install }}
          INPUTS_JSON: ${{ toJSON(github.event.inputs) }}
        run: |
          echo "tf-prepare: $KAFKA_INSTALL_INPUT"
          echo "JSON: $INPUTS_JSON"

      - name: Trigger Kafka install workflow
        uses: actions/github-script@d746ffe35508b1917358783b479e04febd2b8f71 # v9.0.0
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'kafka-new-cluster-install.yml',
              ref: 'main'
            })

  rabbitmq-install:
    permissions:
      actions: write
    needs: tf-post-install
    environment: "${{github.event.inputs.environment}}"
    if: github.event.inputs.rabbitmq-install == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Show inputs
        env:
          RABBITMQ_INSTALL_INPUT: ${{ github.event.inputs.rabbitmq-install }}
          INPUTS_JSON: ${{ toJSON(github.event.inputs) }}
        run: |
          echo "tf-prepare: $RABBITMQ_INSTALL_INPUT"
          echo "JSON: $INPUTS_JSON"

      - name: Trigger Rabbitmq install workflow
        uses: actions/github-script@d746ffe35508b1917358783b479e04febd2b8f71 # v9.0.0
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'rabbitmq-new-cluster-install.yml',
              ref: 'main'
            })

  cassandra-install:
    permissions:
      actions: write
    needs: tf-post-install
    environment: "${{github.event.inputs.environment}}"
    if: github.event.inputs.cassandra-install == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Show inputs
        env:
          CASSANDRA_INSTALL_INPUT: ${{ github.event.inputs.cassandra-install }}
          INPUTS_JSON: ${{ toJSON(github.event.inputs) }}
        run: |
          echo "tf-prepare: $CASSANDRA_INSTALL_INPUT"
          echo "JSON: $INPUTS_JSON"

      - name: Trigger Cassandra install workflow
        uses: actions/github-script@d746ffe35508b1917358783b479e04febd2b8f71 # v9.0.0
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'cassandra-new-cluster-install.yaml',
              ref: 'main'
            })
